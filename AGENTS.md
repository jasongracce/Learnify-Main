# Workspace Instructions

Before starting any Learnify work in this workspace, read these files first:

1. `docs/PRODUCT.md`
2. `docs/ARCHITECTURE.md`

Treat `docs/PRODUCT.md` as the product source of truth and `docs/ARCHITECTURE.md` as the current implementation contract.

Build incrementally toward the first milestone:

Landing page + Supabase waitlist gate + auth + dashboard + one interactive Physics course/module + embedded question blocks + persisted progress + rule-based Lumi chat and insights.
